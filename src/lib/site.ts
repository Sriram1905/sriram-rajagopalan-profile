// Mirrors basePath in next.config.js (single deployment constant, see CLAUDE.md).
// next/link applies it automatically; use this for plain <a> static-file links.
export const BASE_PATH = "/sriram-rajagopalan-profile";
